node_modules/
.next/
out/
coverage/

.env
.env.*
!.env.example

*.log
.DS_Store
# Cache do TypeScript
*.tsbuildinfo

# O projeto nunca versionou lockfile, e a Vercel resolve pelo package.json.
# Versionar agora significaria manter 5 mil linhas em dia por copiar e colar.
package-lock.json

# Amostras da folha impressa, geradas para conferência. Nascem fora do
# projeto; estas linhas existem porque uma vez elas nasceram dentro e um
# `git add -A` as levou para o repositório.
/amostra-*
/pior-caso.*
/A-*.pdf
/B-*.pdf
/modal*.png

# Ruflo local secrets and runtime data
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/
.claude-flow/metrics/
.claude-flow/security/
.claude-flow/policy/
/.swarm/
/ruvector.db
